# Módulo `cluster`

Clúster EKS con un node group gestionado, con los nodos en subredes privadas.

Publica como output la URL del proveedor OIDC del clúster, de la que dependen los roles de IRSA que declara el stack efímero.
