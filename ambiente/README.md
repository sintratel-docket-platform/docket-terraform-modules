# Módulo `ambiente`

Árbol de parámetros en SSM Parameter Store de un ambiente, bajo el prefijo `/docket/<ambiente>/`.

Los valores no se versionan en este repositorio. El módulo crea la estructura, y los secretos se cargan aparte.
