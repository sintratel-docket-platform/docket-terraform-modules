# Matches what the gh-actions image runs, so a local generation and the CI
# check cannot disagree.
formatter: markdown table

recursive:
  enabled: true
  path: .
  include-main: false

output:
  file: README.md
  mode: inject

sort:
  enabled: true
  by: name
